echo "🔵 Running lint-staged (lint + format on staged files)..."

pnpm exec lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Pre-commit checks failed. Commit aborted."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
